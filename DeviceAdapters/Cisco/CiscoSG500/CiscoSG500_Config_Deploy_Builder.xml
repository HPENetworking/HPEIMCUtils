<?xml version="1.0"?>
<definition>

	<action name="deploy_startup_config">
		<step>
			<command name="deploy_startup_config_tftp" description="deploy to startup configuration via TFTP through CLI"/>
		</step>
	</action>
	
	<action name="reload_device">
		<step>
			<command name="reload_device" description="reload device via TFTP through CLI"/>			
		</step>
	</action>
	
		<action name="save_config">
		<step>
			<command name="save_config" description="save config via TFTP through CLI"/>			
		</step>
	</action>

</definition>